{
  "name": "flashlearn-mern",
  "version": "1.0.1",
  "description": "",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install:client": "npm --prefix ./client install",
    "install:server": "npm --prefix ./server install",
    "install:all": "npm-run-all -p install:client install:server",
    "client:build": "npm --prefix ./client run build",
    "client:dev": "npm --prefix ./client run dev",
    "client:dev:dev": "npm --prefix ./client run dev:dev",
    "client:dev:prod": "npm --prefix ./client run dev:prod",
    "client:dev:preview": "npm --prefix ./client run preview",
    "server:start": "npm --prefix ./server start",
    "server:dev": "npm --prefix ./server run dev",
    "dev": "npm-run-all -p client:dev server:dev",
    "dev:server:prod": "npm-run-all -p client:dev:prod server:start",
    "dev:netlify": "netlify dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
